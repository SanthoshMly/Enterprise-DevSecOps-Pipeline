# Scripts must keep LF endings even when checked out on Windows -- a CRLF
# shebang line ("#!/usr/bin/env bash\r") fails with "bad interpreter" on
# Linux/Mac, which is exactly where these scripts and CI actually run.
*.sh text eol=lf
Dockerfile text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
